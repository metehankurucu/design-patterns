import UserAccount from "./UserAccount";

class BlockService {
  checkBlock = (user: UserAccount, blockedUser: UserAccount) => {
    // DB check
    return false;
  };
}

export default BlockService;
