import UserAccount from "./UserAccount";

class FollowService {
  checkFollowing = (user: UserAccount, followedUser: UserAccount) => {
    // DB check
    return true;
  };
}

export default FollowService;
